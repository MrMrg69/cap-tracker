export type ManhuaItem = {
  id: string;
  name: string;
  description: string;
  totalChapters: number;
  currentChapter: number;
  status: string;
};

export type StatItem = {
  label: string;
  value: string;
  trend: string;
};
